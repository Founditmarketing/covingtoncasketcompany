# This is a static client-side marketing site (Vite + React).
# It reads NO environment variables at build time or runtime.
# No API keys, secrets, or env vars are required to run `npm run dev`
# or to deploy to Vercel as a static site.
#
# (The DISABLE_HMR var in vite.config.ts is an optional local dev toggle only.)
